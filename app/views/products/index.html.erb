<h1>Products</h1>
<%= link_to "New product","/products/new" %>

<% @products.each do |product| %>
	
	<p><%= link_to product.name, "/products/#{product.id}" %>
		 <%= product.description %>
		 <%= product.formatted_price %></p>
<% end %>